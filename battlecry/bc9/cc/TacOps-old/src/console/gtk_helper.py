#!/usr/bin/env python

import gtk;

class obj:
    python_is_stupid = True;


#    def gtk_watcher(source, condition, self):
#        if gobject.IO_HUP == condition:
#                connect(self);
#                return TRUE;
#        watcher(self);
#        return TRUE;
#
#    def gtk_watch(self):
#        condition = gobject.IO_IN | gobject.IO_OUT | gobject.IO_PRI;
#        condition |= gobject.IO_ERR | gobject.IO_HUP;
#        gobject.io_add_watch(self.get_sock_fd(), condition, self);
#
